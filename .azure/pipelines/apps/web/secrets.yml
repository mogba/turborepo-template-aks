# apiVersion: spv.no/v2beta1
# kind: AzureKeyVaultSecret

# metadata:
#   name: $(projectName)-$(environmentAcronym)-database-host
#   namespace: "$(k8sNamespace)"

# spec:
#   output:
#     secret:
#       dataKey: $(projectName)-$(environmentAcronym)-database-host
#       name: $(projectName)-$(environmentAcronym)-database-host

#   vault:
#     name: $(azureKeyVaultName)
#     object:
#       name: $(projectName)-$(environmentAcronym)-database-host
#       type: secret
